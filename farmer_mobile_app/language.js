document.title='AgroSmart | Language';document.querySelectorAll('body *').forEach(node=>{if(node.childNodes.length===1&&node.firstChild.nodeType===3)node.textContent=node.textContent.replaceAll('AgriSaathi','AgroSmart')});
const draft=JSON.parse(sessionStorage.getItem('agriSaathiDraft')||'null');if(!draft)window.location.href='login.html';const toast=document.querySelector('#toast');let chosen='';function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}document.querySelectorAll('.language-option').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.language-option').forEach(item=>item.classList.remove('selected'));button.classList.add('selected');chosen=button.dataset.language;document.querySelector('#continueButton').disabled=false}));document.querySelector('#continueButton').addEventListener('click',()=>{if(!chosen){showToast('Choose a language first');return}sessionStorage.setItem('agriSaathiProfile',JSON.stringify({...draft,language:chosen}));sessionStorage.removeItem('agriSaathiDraft');window.location.href='dashboard.html';});