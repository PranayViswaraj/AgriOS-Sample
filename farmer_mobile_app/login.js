document.title='AgroSmart | Login';document.querySelectorAll('body *').forEach(node=>{if(node.childNodes.length===1&&node.firstChild.nodeType===3)node.textContent=node.textContent.replaceAll('AgriSaathi','AgroSmart')});
const form=document.querySelector('#loginForm');const toast=document.querySelector('#toast');function showToast(message){toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}form.addEventListener('submit',event=>{event.preventDefault();const farmerName=document.querySelector('#farmerName').value.trim();const mobile=document.querySelector('#mobileNumber').value.replace(/\D/g,'');const farmName=document.querySelector('#farmName').value.trim();if(!farmerName||mobile.length<10||!farmName){showToast('Please enter your name, farm and 10-digit mobile number');return}sessionStorage.setItem('agriSaathiDraft',JSON.stringify({farmerName,mobile,farmName}));window.location.href='language.html';});